# This file describes the network interfaces available on your system
# and how to activate them. For more information, see interfaces(5).

# The loopback network interface
auto lo
iface lo inet loopback

# WAN interface
auto eth0
iface eth0 inet dhcp
	pre-up iptables -t nat -A POSTROUTING -o $IFACE -j MASQUERADE
	pre-up iptables -A INPUT -i eth0 -p tcp -m tcp --dport 10000 -j DROP 

	post-down iptables -t nat -D POSTROUTING -o $IFACE -j MASQUERADE
	post-down iptables -D INPUT -i eth0 -p tcp -m tcp --dport 10000 -j DROP

# LAN interface (runs DHCP server on this)
auto eth1 
iface eth1 inet static
	address 192.168.100.1
	netmask	255.255.255.0

